Concepts for implementation in the project:

- dependency injection (injectable) ✅
- class-validator (controller validation) ✅
- config service ✅
- module ✅
- exception filter ✅ 
- provider, provider factory ✅
- scopes ( specific to NestJs ) 🤔 - return to it at the end 
- Guard ( use jwt ), middleware, interceptor (and their differences) 
    ( implement guard and add logger middleware, but not interceptor)
- unit tests 🤔?

general todos:
1) index export files
2) Now i need to check if i can pull all of the book borrowed by a user, or all the users that borrowed the book.
- ( Only work on the typeOrm SQL queries that are necessary according to the figma plan )
3) Make sure I Implement comments from both sakads !
4) Enforce eslint, ido gave me an example file
5) Add logger middleware
6) 
7) 

21/11 Todo:
1) Implement Guard, use jwt
2) I need to actually understand the whole db - repository configuration.
3) 

----------------------------------------------------------------------------------------------------------------------------------
